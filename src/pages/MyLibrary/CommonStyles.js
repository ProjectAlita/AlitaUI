import { RIGHT_PANEL_HEIGHT_OFFSET } from '@/common/constants';

export const rightPanelStyle = {
  height: `calc(100vh - ${RIGHT_PANEL_HEIGHT_OFFSET})`, 
  display: 'flex', 
  flexDirection: 'column'
};

export const tagsStyle = { flex: 1 };